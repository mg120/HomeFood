<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.FamilyProductsFrag">

    <ListView
        android:id="@+id/family_products_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="2dp"
        android:layout_marginTop="5dp"
        android:background="@drawable/products_background"
        android:padding="5dp"
        android:scrollbars="vertical"
        android:smoothScrollbar="true"
        android:visibility="gone" />

    <TextView
        android:id="@+id/check_products_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="لا توجد منتجات حالية"
        android:visibility="gone" />

    <ProgressBar
        android:id="@+id/load_prods_progress_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center" />

</FrameLayout>